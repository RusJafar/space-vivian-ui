services:
  client:
    image: client-image
    build:
      context: ./client
      dockerfile: Dockerfile
    ports:
      - "80:3000"
    restart: unless-stopped
  servrer:
    image: backend-image
    build:
      context: ./server
      dockerfile: Dockerfile
    ports:
      - "3001:3001"
    restart: unless-stopped